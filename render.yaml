services:
  - type: web
    name: financial-ai
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python deploy_check.py
    startCommand: gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: FLASK_ENV
        value: production
